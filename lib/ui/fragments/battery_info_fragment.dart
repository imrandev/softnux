import 'package:flutter/material.dart';

class BatteryInfoFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "Battery Info",
        ),
      ),
    );
  }
}
